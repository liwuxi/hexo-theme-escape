<%
    let categories = [];
    item.categories.forEach(function(category){
        categories.push('<a class="category" href="' + config.root + category.path + '">' + category.name + '</a>');
    });
%>
<%- categories.join(' ') %>
